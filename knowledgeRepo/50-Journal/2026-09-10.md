---
type: journal
date: 2026-09-10
tags: [日记]
---

# 2026-09-10

## 今日重点

- 完成 Linux 学习路径执行版阶段 0：搭起可断点调试的 QEMU+GDB 环境，产出源码地图与产物链
- 把「内核源码阅读 / 最小驱动 / QEMU+GDB / 设备模型」整合成 T 线地基，并与 PlutoSDR 真实链路（P 线）合并为 9 阶段执行路线

## 随手捕获

- WSL 的 sudo 需要密码，改用 `apt-get download` + `dpkg-deb -x` 用户态解包 flex/bison/bc/libelf/QEMU，环境写在 `/home/congqiang/work/repo/tools/kernel-lab`
- QEMU 用户态安装必须设置 `QEMU_MODULE_DIR` 指向解包的 `accel-tcg-x86_64.so`，否则会报 `accel_init_ops_interfaces` 断言
- plutosdr-fw 的 Linux 子模块其实是 6.1 内核（`6.1.0-gf3da30df6004`），T 线可以直接用它构建 x86_64 调试内核，不需要另拉 mainline

## 进展 / 学习

- 阶段 0 成果沉淀为 [[学习-Linux-阶段0-源码地图与调试环境]]：环境总览、复现命令、GDB/QEMU 实测证据、源码地图、plutosdr-fw 产物链
- 新增 [[学习-Linux-内核源码阅读与驱动学习法]]（T 线方法：源码地图、最小模块、VFS 调用链、QEMU+GDB、设备模型、资源清单）
- [[学习-Linux-学习路线]] 重构为「优化整合版执行路线（9 阶段）」，原 T/P 清单保留为详细参考；新增 G1–G4 执行闸门与跨主题协同表
- 新增 `scripts/kernel-lab/`：`env.sh`、`init`、`make_initramfs.sh`、`run_qemu_gdb_demo.sh`、`README.md`
- 收集 LKD 书目：新建 [[书籍-Linux内核设计与实现（LKD）]]（中文第 3 版 2011/ISBN 9787111338291；英文 3rd ed. 2010/ISBN 978-0-672-32946-3；含目录与学习路径映射）
- 下载出版社官方试读到 `90-Attachments/书籍/LKD-3rd-edition-sample-chapter2.pdf`（第 2 章 + 索引，约 671 KB；非全书，盗版全书未获取）
- 新建 [[概念-物理层射频驱动全景]]：按 RFIC 驱动 → FPGA/JESD 数据面 → 用户态框架 → RAN radio 后端四层梳理，并挂到 MOC-射频 / MOC-Linux / 领域-L1
- 实测：GDB 断在 `init/main.c:937 start_kernel`，QEMU 串口输出 `INITRAMFS_OK` → `USERSPACE_READY` → `Power down`
- 新增交互式启动 `run_qemu.sh`（cmdline 加 `debugshell`）：已实测进入 guest busybox shell，`uname -a` 为 6.1.0-gf3da30df6004，`poweroff -f` 正常退出；QEMU 同时开放 `:1234` 供 `attach_gdb.sh` 断点调试
- 构建产物：`bzImage` 9.8 MiB、`vmlinux` 356 MiB、`initramfs.cpio.gz` 1.1 MiB

## 复盘

- 做得好：先查工具链与权限，再选择无 sudo 的用户态方案，避免卡在安装；把可复用命令固化成脚本
- 可以改进：阶段 0 的源码地图还偏索引，下一步要选 1–2 个入口函数做“调用链小抄”而不是继续铺图
- 明天：进入执行版阶段 1（hello 模块 + misc 字符设备闭环），跑通 `insmod → cat/echo → dmesg → rmmod`，并把 `read()` → `vfs_read` → `file_operations` 调用链在 GDB 里断一次
