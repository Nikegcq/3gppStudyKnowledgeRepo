---
type: resource
tags: [C++, 面试题, 面向对象]
created: 2026-09-18
updated: 2026-09-18
status: active
---

# C++ 面试 — 语言基础与面向对象

> 适用：通用 C++ 岗 + 协议栈/嵌入式  
> 结构：每题 **口头答案** + **关键点** + **易被追问**  
> 索引：[[C++面试-总览]]

---

## 速记总览

| 题号 | 题目 | 一句话结论 |
|------|------|------------|
| 1 | 指针与引用 | 引用是别名、必须初始化、不可重绑；指针是地址、可空、可改指向 |
| 2 | 虚函数原理 | 动态联编靠对象里 vptr → vtable → 虚函数指针；例子见 [[C++面试-虚函数实现举例详解]] |
| 3 | 纯虚函数与抽象类 | 含纯虚函数的类不可实例化；接口类常用 |
| 4 | 为何基类析构要 virtual | 否则 `delete Base*` 只调 Base 析构，派生资源泄漏/UB |
| 5 | 多态成立条件 | 继承 + 虚函数 + 基类指针/引用调用 + 运行期绑定 |
| 6 | 构造/析构顺序 | 构造：基类→成员→自身；析构相反 |
| 7 | 深拷贝与浅拷贝 | 默认成员拷贝是浅的；有裸指针/资源时必须自定义深拷贝或禁用拷贝 |
| 8 | static 与 const | static 管存储期/链接/类共享；const 管不可改承诺；详见 [[C++面试-static关键字详解]] |

---

## 1. 指针和引用有什么区别？

### 口头答案

引用是对象的别名，定义时必须绑定且之后不能改绑，不存在空引用；指针保存地址，可以为空、可以改指向，也可以有二级指针。底层实现上引用常按指针实现，但语言语义更严：必须解引用才有值，不能做“指向另一对象”的再绑定。

### 关键点

| 维度 | 引用 | 指针 |
|------|------|------|
| 初始化 | 必须 | 可延迟 |
| 空值 | 无 | `nullptr` |
| 重绑 | 不可 | 可以 |
| 运算 | 当对象用 | 算术/多级 |
| 典型用途 | 函数传参避免拷贝 | 可选、多态、动态对象 |

### 易被追问

- 传引用一定更快？→ 大对象是；`int` 等小类型编译器常已优化，收益有限。  
- 引用底层是什么？→ 通常实现为指针，但标准只保证语义。  
- 能返回局部变量引用吗？→ 不能，悬空；可返回 `std::move` 后的成员或静态/堆对象。

---

## 2. 虚函数是如何实现的？

> 深挖 + 代码示例：[[C++面试-虚函数实现举例详解]]

### 口头答案

含虚函数的类，对象里通常多一个 **vptr**，指向该类的 **vtable**；vtable 里按虚函数声明顺序存放函数指针（还可能含 typeinfo 等）。调用 `base->foo()` 时经 vptr 找到当前对象实际类型的表，再取函数地址调用，这就是运行期多态。构造期间 vptr 指向“当前正在构造的类”的表，所以构造函数里调虚函数通常不会走最终派生版本。

### 关键点

```text
对象 → vptr → vtable[slot] → 函数地址
```

最小示例（完整版与迷你 vtable 模拟见深挖笔记）：

```cpp
struct Animal {
  virtual void speak() {}
  virtual ~Animal() = default;
};
struct Dog : Animal {
  void speak() override { /* wang */ }
};
Animal* p = new Dog;
p->speak();  // 读 Dog 对象 vptr → vtable → Dog::speak
```

- 静态联编：非虚函数按调用表达式类型决定  
- 动态联编：虚函数按对象动态类型决定  
- 代价：一次间接跳转 + 对象体积增加 + 可能阻碍内联  

### 易被追问

- 空类多大？→ 通常 1 字节；加虚函数后至少含 vptr（多数平台 8 字节）。  
- 多继承？→ 可能多个 vptr，还有 this 调整（thunk）。  
- 构造里调虚函数？→ vptr 仍指向当前基类版本，见深挖笔记第 4 节。  
- 性能敏感路径能用虚函数吗？→ 可以，但热循环里更倾向模板/`if constexpr`/直接调用，协议栈控制面用虚函数很常见。

---

## 3. 什么是纯虚函数和抽象类？

> 示例与接口风格：[[C++面试-虚函数实现举例详解]] 第 5、7 节

### 口头答案

形如 `virtual void f() = 0;` 的函数是纯虚函数，没有（强制）类内实现。含纯虚函数的类是抽象类，不能实例化，子类必须覆盖后才可构造。抽象类常用来定义接口，让不同协议实现（如不同 MAC 策略）共用同一调用面。

### 关键点

```cpp
struct L2Scheduler {
  virtual ~L2Scheduler() = default;
  virtual void run_slot(int slot) = 0;
};
// L2Scheduler s;           // 错误：抽象类
// L2Scheduler* p = new DummyScheduler;
```

- 抽象类：不可 `new`，可作指针/引用类型  
- 析构函数可以是虚的，抽象类也常提供 `virtual ~Iface() = default`  
- 与 Java `interface` 类似，但 C++ 可含非虚成员与状态  

### 易被追问

- 纯虚函数能有函数体吗？→ 可以在类外定义，子类仍必须覆盖；不常见。  
- 何时用抽象类 vs 模板？→ 运行期可插拔 → 虚接口；编译期确定、零开销 → 模板策略。

---

## 4. 为什么基类析构函数要声明为 virtual？

> 反例演示：[[C++面试-虚函数实现举例详解]] 第 6 节

### 口头答案

若通过 `Base*` 指针 `delete` 一个 `Derived` 对象，而 `Base` 析构不是 virtual，则只调用 `Base::~Base`，派生类成员/资源不会正确销毁，属于未定义行为风险。多态基类提供虚析构，才能先跑完整派生析构链再跑基类析构。

### 关键点

```cpp
struct Base { virtual ~Base() = default; };  // 多态基类
struct Derived : Base { std::vector<int> buf; };
Base* p = new Derived;
delete p;  // 正确：~Derived → ~Base
```

规则：**只要类被设计为经基类指针删除，析构必须 virtual**（或禁止经基类删除）。

### 易被追问

- `unique_ptr<Base>` 呢？→ 同样要求虚析构，否则删除派生对象仍是问题。  
- 没有虚函数的基类？→ 不要多态删除；按值/按确切类型管理。  
- 虚析构成本？→ 对象多 vptr；控制面对象通常可接受。

---

## 5. C++ 多态需要哪些条件？

### 口头答案

四点：① 有继承关系；② 基类声明虚函数；③ 派生类覆盖；④ 通过**基类指针或引用**在运行期调用。若用对象值切片（`Base b = d;`）或直接调非虚函数，则不会产生动态绑定。

### 关键点

```text
继承层次 + virtual + 基类指针/引用 + 运行期调用
→ 动态绑定到实际类型
```

### 易被追问

- 默认参数与虚函数？→ 默认参数按**静态类型**绑定，易踩坑，虚接口尽量别用默认参数。  
- 构造/析构中调虚函数？→ 不会下降到更派生类，因对象类型尚未/已不再完整。

---

## 6. 构造与析构的调用顺序是什么？

### 口头答案

构造：先基类（按继承列表顺序），再成员变量（按**声明顺序**，不是初始化列表书写顺序），最后派生类构造函数体。析构完全相反：先自身析构体，再成员逆序析构，再基类。异常抛出时按已构造的逆序回滚。

### 关键点

```text
构造：Base → members(声明序) → Derived body
析构：Derived body → members(逆声明序) → Base
```

### 易被追问

- 初始化列表写序 vs 声明序？→ 实际以成员声明序为准，列表写序不一致可能警告。  
- 多继承基类顺序？→ 按 `class D : public A, public B` 的基类列表顺序构造。

---

## 7. 深拷贝与浅拷贝的区别？何时必须自定义？

### 口头答案

默认的拷贝构造/赋值是按成员复制：对指针只拷地址，是浅拷贝，两个对象共享同一资源，析构可能 double-free。类管理独占资源（堆内存、fd、非共享锁等）时必须：实现深拷贝，或**删除拷贝**只允许移动，或用智能指针表达共享/独占所有权。

### 关键点

| 策略 | 含义 | 现代做法 |
|------|------|----------|
| 深拷贝 | 复制资源本体 | 值语义容器（`std::vector` 等） |
| 共享 | 共享所有权 | `shared_ptr` |
| 独占移动 | 资源跟随对象 | `unique_ptr` + 移动语义 |
| 禁止 | 不可复制 | `= delete` |

### 易被追问

- Rule of Three/Five？→ 有任一拷贝控制/析构，通常要齐套或全禁。  
- 协议栈缓冲区？→ 优先值语义大块内存 + 移动，避免浅拷贝共享指针。

---

## 8. static 和 const 分别控制什么？

> 深挖：[[C++面试-static关键字详解]]（局部/文件/类 static、链接属性、线程安全初始化）

### 口头答案

`const` 是**不可修改**的承诺：修饰变量、成员函数（不改对象逻辑状态）、指针指向等。`static` 控制**存储期与链接/作用域**：局部 static 跨调用存活；文件作用域 static 是内部链接；类 static 成员/函数归类所有、无 `this`（C++17 可 inline 定义）。两者常组合：`static const`/`static constexpr` 类内常量、`static` 方法工具函数。

### 关键点

| 场景 | 效果 |
|------|------|
| `const int` / `const T*` | 值或被指对象不可改 |
| `T* const` | 指针本身不可改指向 |
| 局部 `static` | 函数结束仍存活，线程安全初始化（C++11） |
| 文件作用域 `static` | 内部链接，仅本翻译单元可见 |
| 类 `static` 成员 | 所有对象共享，不在对象布局内 |
| 类 `static` 成员函数 | 无 `this`，不可 `virtual` |
| `const` 成员函数 | 可在 const 对象上调用，不修改可观察状态 |

### 易被追问

- `const` 成员函数能改成员吗？→ 可 `mutable` 缓存；逻辑上仍应视为不可变。  
- `static` 全局与匿名命名空间？→ 匿名命名空间更利于封装、避免符号冲突；现代 C++ 优先。  
- 局部 static 线程安全吗？→ **初始化**安全；之后读写仍可能要锁/原子。  
- 更多追问清单与工程取舍 → [[C++面试-static关键字详解]]

---

## 面试答法示例

> 多态靠虚函数表：对象里 vptr 指向 vtable，调用时按动态类型取函数指针。多态基类析构必须 virtual，否则 delete 基类指针会漏掉派生资源。项目里协议栈的策略接口常用抽象类；热路径数据对象则避免虚函数，用具体类型和移动语义。static 要分场景：函数内管存储期，文件作用域管内部链接，类内属于类且静态函数无 this。

---

## 相关

- [[C++面试-总览]]
- [[C++面试-虚函数实现举例详解]]
- [[C++面试-static关键字详解]]
- [[C++面试-内存管理与智能指针]]
- [[C++面试-现代C++特性]]
- [[MOC-C++与软件]]
